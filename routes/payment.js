const express = require("express");
const router = express.Router();

module.exports = (User, Payment) => {
  // Define your payment-related routes here
  // e.g., router.post("/create", async (req, res) => { ... });

  return router;
};